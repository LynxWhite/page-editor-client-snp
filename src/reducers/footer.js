import {} from '../constants/ActionTypes';

const initialState = [

];

export default function footer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}